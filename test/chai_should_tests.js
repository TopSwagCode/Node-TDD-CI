/*
    Test using Mocha with Chai.
    Style using chai Should eg:
        foo.should.be.a('string');
        foo.should.equal('bar');
        foo.should.have.length(3);
        tea.should.have.property('flavors').with.length(3);
*/
const chai = require('chai');

chai.should();
